// Code goes here

var papa_parse = require('.papa_parse.min.js');
console.log(papa_parse);
// instant crawler
var iCrawler = {
  
};

// traditional crawler
var tCrawler = {
  
};

// convert csv to json
var csvToJson = {
  
};

// json to chunks
var jsonToChunks = {
  
};

// parse chunks
var parseChunks = {
  
};

// filter information
var filterInfo = {
  
};

// ajax fetch
var xhrf = {
  
};

// handle uploads
var hUploads = {
  
};